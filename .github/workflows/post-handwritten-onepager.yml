name: Handwritten one-pager (Mon 10 AM IST)
# Posts content/handwritten-onepager/<YYYY-MM-DD>.png committed in advance by you.
# Exits silently if no file exists for today — no failure, no issue opened.

on:
  workflow_dispatch:        # scheduled by cron-job.org (Mon 10 AM IST)

permissions:
  contents: write
  issues: write

concurrency:
  group: ig-bot-content
  cancel-in-progress: false

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci || npm install
      - name: Post handwritten one-pager
        env:
          IG_USER_ID:      ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
        run: npm run post:onepager
      - name: Open issue on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner, repo: context.repo.repo,
              title: `posting-failure: handwritten one-pager (${new Date().toISOString().slice(0,10)})`,
              labels: ["posting-failure"],
              body: `Workflow [${context.workflow}](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}) failed.`
            });
